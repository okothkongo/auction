<h1> Available Items </h1>
<ul>
<%= for item <- @items do %>
<li>
  <strong><%= link(item.title, to: Routes.item_path(@conn, :show, item))%></strong> -
   <%= item.description %> -
   <%= link("delete", to: "/items/#{item.id}", method: :delete)%>
</li>
<% end %>
<ul>
<p><%=button("Add New Item", to: "/items/new", method: :get)%></p>